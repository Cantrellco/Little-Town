<main class="section"><div class="container reveal"><div class="center"><h1>Order {{ order.name }}</h1></div><p class="center">{{ order.created_at | date: '%B %-d, %Y' }} — {{ order.total_price | money }}</p></div></main>
